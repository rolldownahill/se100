// const age = 20;

// if (age > 14) {
//     beverage = 'Beer';
// } else {
//     beverage = 'Juice';
// }
// console.log(beverage); // 'Beer' if age is 18 or more, 'Juice' otherwise

const age = 20;
const beverage = age > 14 ? 'Beer' :'Juice'
console.log(beverage); // 'Beer' if age is 18 or more, 'Juice' otherwise